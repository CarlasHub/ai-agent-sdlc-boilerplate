# Claude Instructions

Follow `AGENTS.md` and the AI-Agent SDLC governance documents.

Before implementation, validate governance with:

```bash
npm run governance:check
```

If governance is incomplete, stop and request completion. Do not generate implementation code to bypass the process.
